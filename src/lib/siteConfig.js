/**
 * Famies site mode.
 *
 * Set LIVE = false to show the temporary "Coming Soon" landing.
 * Set LIVE = true to instantly restore the full redesigned site
 * (Hero, PainPoints, HowItWorks, Features, SocialProof, Download,
 *  Blog, Newsletter, Navbar, Footer).
 *
 * Flip this single boolean and redeploy to switch — no other files
 * need to change.
 */
export const LIVE = false;
